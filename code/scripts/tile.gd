extends TextureRect

@export var tile_index : int ##this determines which tile of the given tile_palette
@export var tile_palette_library : Array[Resource] ##all of the tilesets in the for of their own arrays, each with its own index
